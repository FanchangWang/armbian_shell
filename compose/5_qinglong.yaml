name: qinglong

services:
  qinglong:
    image: whyour/qinglong:latest
    container_name: qinglong
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - /opt/qinglong:/ql/data
    restart: unless-stopped
    ports:
      - "5700:5700"
